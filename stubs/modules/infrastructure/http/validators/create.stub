{{#var modelName = generators.modelName(name)}}
{{#var kebabName = string(modelName).snakeCase().toString().replaceAll('_','-')}}
{{#var className = 'Create' + modelName + 'Validator'}}
{{#var paramFolder = string(name).camelCase().toString()}}

{{{
  exports({
    to: app.makePath('app/modules', paramFolder, 'infrastructure/http/validators', 'create-' + kebabName + '.validator.ts')
  })
}}}
import vine from '@vinejs/vine'

export const {{ className }} = vine.compile(
  vine.object({
    // Add your fields here
  })
)
